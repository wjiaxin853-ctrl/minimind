### 预训练命令
```bash
CUDA_VISIBLE_DEVICES=0,1 torchrun --nproc_per_node 2 train_pretrain.py \
  --dtype bfloat16 \
  --batch_size 32 \
  --accumulation_steps 8 \
  --max_seq_len 340 \
  --num_workers 4 \
  --log_interval 20 \
  --save_interval 500 \
  --use_wandb \
  --wandb_project MiniMind-Pretrain-2x3090 \
  --learning_rate 5e-4
```  

  | 参数 | 作者默认基线 | 你现在推荐值 | 说明 |
  |---|---:|---:|---|
  | GPU | 1 卡默认 | 2 张 3090 | 用 torchrun --nproc_per_node 2 |
  | device | cuda:0 | 由 torchrun 自动分配 | 多卡时不要手写单卡 --device |
  | epochs | 2 | 2 | 保持一致 |
  | batch_size | 32 | 8 | 两卡先保守，稳一点 |
  | accumulation_steps | 8 | 8 | 保持一致 |
  | 等效 batch | 32*8*1=256 | 8*8*2=128 | 你的推荐值更保守 |
  | learning_rate | 5e-4 | 5e-4 | 先不动 |
  | dtype | bfloat16 | bfloat16 | 3090 可用，优先这个 |
  | num_workers | 8 | 4 | 两卡够用，也更稳 |
  | max_seq_len | 340 | 340 | 保持一致 |
  | use_moe | 0 | 0 | 先不要开 |
  | data_path | ../dataset/pretrain_t2t_mini.jsonl | ../dataset/pretrain_t2t_mini.jsonl | 保持一致 |
  | log_interval | 100 | 20 | 更方便盯训练进度 |
  | save_interval | 1000 | 500 | 更频繁保存，适合远端训练 |
  | use_wandb | 默认关闭 | 开启 | 项目里实际走的是 swanlab 兼容接口 |

##### swanlab链接
https://swanlab.cn/@deepcv-wang/MiniMind-Pretrain-2x3090/runs/ckl816sybovk9shx0t1e7/chart

##### 测试预训练模型
```bash
python eval_llm.py --load_from ./model --weight pretrain --device cuda:0
```


### 指令微调

```bash
CUDA_VISIBLE_DEVICES=0,1 torchrun --nproc_per_node 2 train_full_sft.py \
  --dtype bfloat16 \
  --batch_size 16 \
  --accumulation_steps 4 \
  --max_seq_len 768 \
  --num_workers 4 \
  --log_interval 20 \
  --save_interval 500 \
  --use_wandb \
  --wandb_project MiniMind-Full-SFT-2x3090
```
```bash
CUDA_VISIBLE_DEVICES=0,1 torchrun --nproc_per_node 2 train_full_sft.py --dtype bfloat16 --batch_size 16 --accumulation_steps 4 --max_seq_len 768 --num_workers 4 --log_interval 20 --save_interval 500 --use_wandb --wandb_project MiniMind-Full-SFT-2x3090
```

  | 参数 | 作者默认基线 | 你现在推荐值 | 说明 |
  |---|---:|---:|---|
  | GPU | 1 卡默认 | 2 张 3090 | 用 `torchrun --nproc_per_node 2` |
  | device | cuda:0 | 由 `torchrun` 自动分配 | 多卡时不要手写单卡 `--device` |
  | epochs | 2 | 2 | 保持一致 |
  | batch_size | 16 | 16 | 两卡下这个值比较均衡 |
  | accumulation_steps | 1 | 4 | 适当放大等效 batch，同时保留显存余量 |
  | 等效 batch | 16*1*1=16 | 16*4*2=128 | 你的推荐值更适合两卡稳定训练 |
  | learning_rate | 1e-5 | 1e-5 | 先不动，SFT 阶段不建议激进加大学习率 |
  | dtype | bfloat16 | bfloat16 | 3090 可用，优先这个 |
  | num_workers | 8 | 4 | 两卡够用，也更稳 |
  | max_seq_len | 768 | 768 | 保持一致，适合 SFT 多轮对话样本 |
  | use_moe | 0 | 0 | 先不要开 |
  | data_path | ../dataset/sft_t2t_mini.jsonl | ../dataset/sft_t2t_mini.jsonl | 保持一致 |
  | from_weight | pretrain | pretrain | 默认接着预训练权重继续训 |
  | log_interval | 100 | 20 | 更方便盯训练进度 |
  | save_interval | 1000 | 500 | 更频繁保存，适合远端训练 |
  | use_wandb | 默认关闭 | 开启 | 项目里实际走的是 swanlab 兼容接口 |
